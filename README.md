# Experiment-18
## Aim of Experiment: 
Exploring Statistical and Specialized Data Visualization
## Theory:
- Performs exploratory data analysis (EDA) and visualization using Python
- Uses pandas (pd) for data manipulation and structured data handling
- Uses numpy (np) for numerical computations
- Uses matplotlib.pyplot (plt) for plotting graphs
- Uses seaborn (sns) for statistical visualization
- Uses random for generating reproducible random values
- Imports libraries using import pandas as pd, import numpy as np, import matplotlib.pyplot as plt, import seaborn as sns, import random
- Sets random seed using random.seed(value) for reproducibility
- Loads dataset using pd.read_csv("file_path")
- Displays dataset using data.head()
- Checks dataset structure using data.info()
- Generates statistical summary using data.describe()
- Checks missing values using data.isnull()
- Calculates missing values using data.isnull().sum()
- Removes unnecessary columns using data.drop(['column'], axis=1)
- Accesses column using data['column']
- Filters data using data[data['column'] == value]
- Sorts data using data.sort_values(['column'], ascending=False)
- Groups data using data.groupby("column").mean()
- Counts frequency using value_counts()
- Finds correlation using data.corr()
- Creates line plot using plt.plot()
- Creates bar plot using plt.bar()
- Creates histogram using plt.hist()
- Creates scatter plot using plt.scatter()
- Uses sns.heatmap(data.corr()) for correlation visualization
- Displays plots using plt.show()
- Performs data cleaning, analysis, and visualization
- Helps identify patterns and relationships in dataset
- Demonstrates complete workflow of data preprocessing and analysis
- Provides visual insights using matplotlib and seaborn
## Conclusion: 
Demonstrates exploratory data analysis and visualization using Python libraries like pandas, matplotlib, and seaborn.  
